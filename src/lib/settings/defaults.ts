/**
 * Single source of truth for default branding (keep aligned with globals.css :root --brand-hex-*).
 */
export const BRAND_HEX = {
  start: '#9F001B',
  mid: '#58143B',
  end: '#1B2556',
} as const;

export const DEFAULT_SESSION_MINUTES = 60;
